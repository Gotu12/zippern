<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Prevents transitive manifests from re-introducing SMS (Play / Protect). Merger may warn if none merged. -->
    <uses-permission android:name="android.permission.READ_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" tools:node="remove" />

    <!-- required=false: app can install and run on devices without a camera (avoids install/runtime edge cases). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- Snap Camera Kit / lens beauty shaders expect OpenGL ES 3.0+ for correct rendering. -->
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Gallery: API ≤32 uses READ_EXTERNAL_STORAGE; API 33+ uses READ_MEDIA_IMAGES (Photo Picker). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 14+: partial access / Photo Picker user selection (Play policy–friendly). -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <attribution
        android:tag="camera_kit"
        android:label="@string/app_name" />

    <application
        android:name=".DatingApp"
        android:extractNativeLibs="true"
        android:allowBackup="true"
        android:allowNativeHeapPointerTagging="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.DatingApp">
        <!-- Snap Camera Kit: token placeholder from Gradle ([local.properties]); see Snap Developer Portal -->
        <meta-data
            android:name="com.snap.camerakit.api.token"
            android:value="${snapCameraKitApiToken}" />
        <activity
            android:name="com.zipper.datingapp.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.Starting"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep-link from push notifications: calls, messages, live streams. -->
            <intent-filter>
                <action android:name="com.zipper.datingapp.action.OPEN_INCOMING_CALL" />
                <action android:name="com.zipper.datingapp.action.DECLINE_CALL" />
                <action android:name="com.zipper.datingapp.action.OPEN_MESSAGES" />
                <action android:name="com.zipper.datingapp.action.OPEN_LIVE" />
                <action android:name="com.zipper.datingapp.action.OPEN_AUDIO_PARTY_LOBBY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.zipper.datingapp.LiveStreamActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DatingApp"
            android:windowSoftInputMode="adjustResize" />

        <!-- exported MUST be false for MESSAGING_EVENT per Firebase / Android security; not a public entry point. -->
        <service
            android:name=".service.CallFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Android 14+: declare FGS types used during calls / live (camera + microphone). -->
        <service
            android:name=".service.CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <!-- Receives [Service.onTaskRemoved] when the user swipes the app from Recents (Activity has no public equivalent). -->
        <service
            android:name=".service.LiveTaskCleanupService"
            android:exported="false"
            android:stopWithTask="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>